[build-system]
requires = ["maturin>=1.3,<2"]
build-backend = "maturin"

[project]
name = "linuxtoys-gui"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "app._gui_rs"
features = ["pyo3/extension-module"]
