pkgname='linuxtoys'
pkgver='1.0'
pkgrel=1
pkgdesc='A set of tools for Linux presented in a user-friendly way'
arch=('x86_64')
url='https://git.linux.toys/psygreg/linuxtoys'
license=('GPL3')
depends=(bash git curl wget zenity gtk3 vte3)
makedepends=()
conflicts=(linuxtoys-bin)
provides=("linuxtoys=$pkgver")
source=("https://github.com/psygreg/linuxtoys/releases/download/${pkgver}/linuxtoys-${pkgver}.tar.xz")
sha256sums=('b45db155c7ee6b0e78e7ef1126267882929df672af53c5b16000de44d6e614e8')

package() {
    mkdir -p ${pkgdir}/usr
    cp -rf ${srcdir}/linuxtoys-${pkgver}/usr/* ${pkgdir}/usr/

    # Set proper permissions for executable files
    chmod +x "${pkgdir}/usr/bin/linuxtoys"
    chmod +x "${pkgdir}/usr/share/linuxtoys/linuxtoys.bin"
}
